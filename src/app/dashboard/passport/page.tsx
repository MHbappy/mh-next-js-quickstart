import { PassportListPage } from '@/features/passport/components/passport-list-page';

export const metadata = {
  title: 'Passport | Dashboard',
  description: 'Manage passport records'
};

export default function EmployeesPage() {
  return <PassportListPage />;
}
